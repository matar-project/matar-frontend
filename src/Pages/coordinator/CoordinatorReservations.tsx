import { CoordinatorReservationsContent } from '../../Components/coordinator/CoordinatorReservationsContent';

export default function CoordinatorReservations() {
  return <CoordinatorReservationsContent />;
}
